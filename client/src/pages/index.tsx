import { useEffect } from "react";
import { Layout } from "../components/Layout";

const Home = () => {
  useEffect(() => {}, []);

  return (
    <Layout>
      <ul></ul>
    </Layout>
  );
};

export default Home;
